# Copy this file to `.env` and fill in your key. `.env` is git-ignored.
# Google Maps Platform key with "Street View Static API" enabled (billing on).
# Optional, for cleaner aerial geo-referencing: also enable "Maps Static API".
GMAPS_KEY=AIza...your_key_here
